/**
 * PrimaryCTA Demo Page
 * 
 * A standalone page to test and verify the PrimaryCTA component.
 * Navigate to /primary-cta-demo to view this page.
 */

import { PrimaryCTAExamples } from '@/components/PrimaryCTA.example'

export default function PrimaryCTADemo() {
  return <PrimaryCTAExamples />
}
